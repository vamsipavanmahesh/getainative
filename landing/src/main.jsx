import React from 'react';
import ReactDOM from 'react-dom/client';
import AINativeSDLC from './AINativeSDLC';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <AINativeSDLC />
  </React.StrictMode>
);
